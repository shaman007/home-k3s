apiVersion: v1
kind: Service
metadata:
  name: stash-headless
  namespace: stash
  labels:
    app: stash
    app.kubernetes.io/name: stash
    app.kubernetes.io/instance: stash
    app.kubernetes.io/managed-by: argoCD
spec:
  clusterIP: None
  selector:
    app: stash
  ports:
    - name: http
      port: 9999
      targetPort: http

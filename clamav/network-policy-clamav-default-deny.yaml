# Baseline policy for ClamAV workload.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: clamav-default-deny
  namespace: clamav
spec:
  podSelector:
    matchLabels:
      app: clamav
  policyTypes:
    - Ingress
    - Egress

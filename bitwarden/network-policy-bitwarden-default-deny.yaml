# Baseline network policy for Bitwarden pods.
# This enforces least privilege while keeping required integrations reachable.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: bitwarden-default-deny
  namespace: bitwarden
spec:
  podSelector:
    matchLabels:
      service: bitwarden
  policyTypes:
    - Ingress
    - Egress

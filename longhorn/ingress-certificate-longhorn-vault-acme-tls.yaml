---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: longhorn-vault-acme-tls
  namespace: longhorn-system
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  secretName: longhorn-vault-acme-tls
  duration: 720h
  renewBefore: 168h
  dnsNames:
    - longhorn.w386.k8s.my.lan
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: vault-acme

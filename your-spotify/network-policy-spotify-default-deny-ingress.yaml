# Namespace-level default deny for ingress in the Spotify namespace.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: spotify-default-deny-ingress
  namespace: spotify
spec:
  podSelector: {}
  policyTypes:
    - Ingress

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
  name: mongo-nfs
  namespace: db
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  resources:
    requests:
      storage: 100Mi

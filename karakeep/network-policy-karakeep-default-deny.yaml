# Baseline network policy for Karakeep in namespace karakeep.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: karakeep-default-deny
  namespace: karakeep
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: karakeep-allow-ingress-from-traefik
  namespace: karakeep
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: traefik
      ports:
        - protocol: TCP
          port: 3000

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: plex
  name: plex-lib
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 15Ti

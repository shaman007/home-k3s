apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ftp
  namespace: camera
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 128Gi

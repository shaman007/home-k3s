apiVersion: v1
kind: Service
metadata:
  name: harbor-webhook-bridge
  namespace: wazuh
spec:
  selector:
    app: harbor-webhook-bridge
  ports:
    - name: http
      port: 80
      targetPort: 8080

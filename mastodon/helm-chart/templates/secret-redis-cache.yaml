{{- if .Values.redis.cache.enabled }}
{{- if and (not .Values.redis.cache.existingSecret) .Values.redis.cache.password }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mastodon.secrets.redisCacheName" . }}
  labels:
    {{- include "mastodon.labels" . | trim | nindent 4 }}
    {{- include "mastodon.userLabels" . | trim | nindent 4 }}
type: kubernetes.io/basic-auth
data:
  password: {{ .Values.redis.cache.password | b64enc }}
{{- end }}
{{- end }}

---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vault-secret-store
  namespace: matrix
spec:
  provider:
    vault:
      server: "http://vault.w386.k8s.my.lan"
      path: "kv"
      version: "v2"
      auth:
        tokenSecretRef:
          name: vault-synapse
          key: token

loki:
  commonConfig:
    replication_factor: 1
  storage:
    type: 'filesystem'
  auth_enabled: false
singleBinary:
  replicas: 1
table_manager:
  enabled: true
  retention_deletes_enabled: true
  retention_period: 744h

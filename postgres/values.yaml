---
primary:
  resources:
    requests:
      cpu: "24m"
      memory: "100Mi"
    limits:
      cpu: "300m"
      memory: "100Mi"
auth:
  username: "postgresql"
  database: "ttrss"
persistence:
   storageClass: "longhorn"
   size: 1Gi
metrics:
  enabled: true
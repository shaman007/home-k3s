# Baseline network policy for Minecraft in namespace minecraft.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: minecraft-default-deny
  namespace: minecraft
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress

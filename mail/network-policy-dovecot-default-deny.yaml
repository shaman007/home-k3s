# Dovecot: default deny, then allow required service traffic and DNS.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: dovecot-default-deny
  namespace: mail
spec:
  podSelector:
    matchLabels:
      app: dovecot
      tier: mail
  policyTypes:
    - Ingress
    - Egress

apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: sync-le-tls
  namespace: mail
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "create", "update", "patch"]

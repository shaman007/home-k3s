apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: sync-le-tls
  namespace: mail
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["letsencrypt-prod"]
    verbs: ["get", "patch"]

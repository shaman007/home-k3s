apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  namespace: mail
  name: dovecot-imaps
spec:
  entryPoints:
    - imaps
  routes:
    - match: HostSNI(`*`)
      services:
        - name: dovecot-tls
          port: 993

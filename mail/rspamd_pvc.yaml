apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: mail
  name: rspamd-local
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 500Mi
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: mail
  name: mail-nfs
  labels:
    storage.k8s.io/name: nfs
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 500Gi
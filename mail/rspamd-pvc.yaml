---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: mail
  name: rspamd-local
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1073741824

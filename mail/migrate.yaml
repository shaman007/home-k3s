apiVersion: v1
kind: Pod
metadata:
  name: migrate1
  namespace: mail
spec:
  containers:
  - name: volume-migrate
    image: nginx:stable-alpine
    imagePullPolicy: IfNotPresent
    volumeMounts:
    - name: volv1
      mountPath: /from
    - name: volv
      mountPath: /to
    ports:
    - containerPort: 80
  volumes:
  - name: volv1
    persistentVolumeClaim:
      claimName: mail-nfs
  - name: volv
    persistentVolumeClaim:
      claimName: mail-lh
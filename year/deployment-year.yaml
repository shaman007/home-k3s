apiVersion: apps/v1
kind: Deployment
metadata:
  name: year
  namespace: year
  labels:
    app: year
    app.kubernetes.io/instance: year
    app.kubernetes.io/name: year
    app.kubernetes.io/managed-by: argoCD
spec:
  replicas: 1
  selector:
    matchLabels:
      app: year
  template:
    metadata:
      labels:
        app: year
    spec:
      containers:
        - name: year
          image: harbor.andreybondarenko.com/library/year-vas3k-cloud:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          readinessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
          livenessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 2
          resources:
            requests:
              cpu: 50m
              memory: 80Mi
            limits:
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: year-data
              mountPath: /tmp
            - name: app-cache
              mountPath: /app/node_modules/.cache
            - name: npm-logs
              mountPath: /root/.npm/_logs
            - name: nginx-cache
              mountPath: /var/cache/nginx
      volumes:
        - name: year-data
          emptyDir: {}
        - name: app-cache
          emptyDir: {}
        - name: npm-logs
          emptyDir: {}
        - name: nginx-cache
          emptyDir: {}

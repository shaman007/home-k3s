---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus-vault-acme-tls
  namespace: monitoring
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  secretName: prometheus-vault-acme-tls
  duration: 720h
  renewBefore: 168h
  dnsNames:
    - prometheus.w386.k8s.my.lan
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: vault-acme

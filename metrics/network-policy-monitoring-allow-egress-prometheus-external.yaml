apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: monitoring-allow-egress-prometheus-external
  namespace: monitoring
spec:
  podSelector:
    matchLabels:
      app: prometheus-server
  policyTypes:
    - Egress
  egress:
    # External Prometheus and the Fedora workstation node exporter.
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 9090
        - protocol: TCP
          port: 9100
    - to:
        - ipBlock:
            cidr: ::/0
      ports:
        - protocol: TCP
          port: 9090
        - protocol: TCP
          port: 9100

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: wordpress-allow-ingress-to-mysql-wordpress-sidecar-from-monitoring
  namespace: wordpress
spec:
  podSelector:
    matchLabels:
      app: mysql-wordpress
      tier: mysql
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
      ports:
        - protocol: TCP
          port: 9104
